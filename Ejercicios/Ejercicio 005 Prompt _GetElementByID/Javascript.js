function calcularIMC(peso, altura)
{
let imc = Peso/((Altura/100)**2); // Aquí lo que hacemos es colocar las variables, poner la altura al cuadrado
// además dividimos tambien el peso por la altura
return imc;
}